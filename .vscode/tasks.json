{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build project linux",
            "type": "shell",
            "command": "sudo g++ main.cpp -lGL -lGLU -lglut",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}